<%
// Inserts a definition list of the subpages of the current page, with each
// page's title as the <dt> term and its SEO summary as the <dd> term. The
// List is presented in a two-column view where possible.
//
// Parameters:
//  Same as SubpagesWithSummaries (an array of Page objects to list instead
//  of subpages)

// Ask SubpagesWithSummaries for the formatted page list, given
// the parameters specified.
var html = template("SubpagesWithSummaries", arguments);

// Now we have the entire list in HTML. Let's go through it and try to 
// split it into two columns by force.
%>

<%-template("MakeColumnsForDL", [html])%>
